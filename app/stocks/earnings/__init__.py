"""The earnings feature area — vertical slices about a stock's earnings.

Currently the ``quarterly`` sub-slice (per-quarter reported EPS + surprise and the
forward quarters). Kept as its own package so future earnings concerns (e.g. annual
history) can sit beside it without crowding ``app/stocks``.
"""
